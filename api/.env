# Database (shared by both containers)
DB_NAME=productdb
DB_USER=postgres
DB_PASSWORD=root
DB_PORT=5432
DB_POOL_SIZE=10

# Spring profile to run on the server
SPRING_PROFILES_ACTIVE=prod

# App internal port
PORT=8080
